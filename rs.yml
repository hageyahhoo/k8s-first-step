apiVersion: apps/v1beta2
kind: ReplicaSet
metadata:
  name: web-service
spec:
  replicas: 5
  selector:
    matchLabels:
      run: web-service
  template:
    metadata:
      labels:
        run: web-service
    spec:
      containers:
      - name: web-service
        image: nginx:latest
        ports:
        - containerPort: 443
        - containerPort: 80
